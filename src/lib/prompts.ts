import type { Identity, OutputFormat } from "@/types/analysis";

const identityGuides: Record<Identity, string> = {
  上级: [
    "目标：把对方表达翻译为【可执行的交付计划】，让上级一眼看到你在推进。",
    "关注点优先级：交付物 > 截止时间/节奏 > 验收标准 > 风险与依赖 > 你下一步动作。",
    "必须识别并写清：",
    "1) 交付物形式（文档/表格/代码/汇报PPT/方案/数据/结论）；",
    "2) 完成标准/验收口径（做到什么算完成）；",
    "3) 时间点（最晚什么时候/分阶段同步时间）；",
    "4) 你需要的输入/依赖（资料、权限、他人配合）；",
    "5) 你承诺的下一步动作（具体到今天/明天做什么）。",
    "不确定就写“需要确认”，不要脑补上级意图或压力来源。",
    "回复风格：简洁、主动、带时间点、带同步节奏，避免空话（如“好的我看下”）。",
  ].join("\n"),

  客户: [
    "目标：需求澄清 + 预期管理 + 可交付承诺（但避免过度承诺）。",
    "关注点优先级：需求边界/口径 > 交付节奏 > 风险暴露 > 需要客户提供的信息 > 下一步确认。",
    "必须识别并写清：",
    "1) 客户到底要的结果是什么（输出形态）；",
    "2) 必做项 vs 可选项（边界：不做什么/后续迭代什么）；",
    "3) 时间与里程碑（先给草稿/先给结论/先给demo）；",
    "4) 需要客户提供的材料/确认点。",
    "不确定必须写“需要确认”，并给出具体确认问题，避免擅自承诺。",
    "回复风格：稳妥、礼貌、可执行；尽量用“计划/建议/可以先…”而不是“一定/保证”。",
  ].join("\n"),

  同事: [
    "目标：让协作能推进，明确分工、依赖、下一步同步方式。",
    "关注点优先级：分工边界 > 依赖与阻塞 > 交付节奏 > 对齐口径。",
    "必须识别并写清：",
    "1) 我负责什么；2) 对方负责什么；3) 需要谁提供什么；4) 下一次同步时间/方式。",
    "不确定就写“需要确认”，不要替同事下结论或扣帽子。",
    "回复风格：自然直接、可落地，优先给出 2-3 个可选时间点或可选方案。",
  ].join("\n"),

  家人朋友: [
    "目标：先处理情绪与关系，再给具体行动建议（不要太“职场化/冷冰冰”）。",
    "关注点优先级：对方情绪/关系维护 > 边界表达 > 实际行动建议。",
    "必须识别并写清：对方想要的是安慰/陪伴/建议/帮忙/拒绝的边界。",
    "不确定就写“需要确认”，不要过度分析动机。",
    "回复风格：温和、真诚、具体（给出可执行的下一步），避免说教。",
  ].join("\n"),

  对象: [
    "目标：减少误解、提升安全感、明确需求与边界。",
    "关注点优先级：情绪/感受 > 误解点 > 具体诉求 > 下一步约定。",
    "必须识别并写清：对方在意的点是什么（被忽视/不被理解/缺少陪伴/不确定性）。",
    "不确定就写“需要确认”，避免推断对方人格或过去经历。",
    "回复风格：真诚、具体、有安抚；避免冷分析腔，尽量用“我理解你…”“我会…”并给出时间安排。",
  ].join("\n"),
};

function replyGuide(outputFormat?: OutputFormat) {
  if (outputFormat === "微信") {
    return [
      "需要生成 reply（微信格式）。要求：",
      "- 80~180 字左右，像真实聊天；",
      "- 先确认理解，再给行动/时间点，再补 1-3 个确认问题（如需要）；",
      "- 不要长段落，最多 3-5 句；",
      "- 不要使用 Markdown/序号/标题。",
    ].join("\n");
  }
  if (outputFormat === "邮件") {
    return [
      "需要生成 reply（邮件格式）。要求：",
      "- 必须包含：称呼 + 正文 + 结尾；",
      "- 语气正式、清晰，避免口语化表情；",
      "- 必须包含：你将如何推进 + 时间点/同步节奏 + 需要对方确认/提供的信息；",
      "- 可用简短分段，但不要 Markdown 标题。",
    ].join("\n");
  }
  return "用户没有选择回复输出形式，reply 必须返回 null。";
}

type BuildAnalyzePromptInput = {
  message: string;
  hasImage: boolean;
  identity: Identity;
  outputFormat?: OutputFormat;
  personName?: string;
  personProfile?: string;
};

export function buildAnalyzePrompt({
  message,
  hasImage,
  identity,
  outputFormat,
  personName,
  personProfile,
}: BuildAnalyzePromptInput) {
  const visionHint =
    hasImage && process.env.AI_ENABLE_VISION === "true"
      ? "本次输入包含截图：请先识别截图内文字、参与者身份（谁说的）、关键上下文；若截图文字不清晰，明确说明“截图内容不清晰，需要用户补充文字”。再结合用户补充文字分析。"
      : "本次输入为纯文字：请基于文字内容与上下文分析。";

  const personHint = personName
    ? [
        `本次指定具体人物：${personName}`,
        personProfile
          ? `该人物已有画像（供你参考，生成 reply 时要符合其偏好与雷区）：\n${personProfile}`
          : "该人物暂无画像：请从本次内容提取“可长期复用”的沟通特征，输出到 personProfileUpdate（不要记录隐私细节或一次性任务内容）。",
      ].join("\n")
    : "本次未指定具体人物：personProfileUpdate 必须返回 null。";

  return [
    "你是一个中文沟通意图分析助手。",
    "你要处理的输入可能来自：聊天记录、邮件正文、单方任务叙述、截图（微信/飞书/邮件截图）。",
    visionHint,
    "",
    "【总原则】",
    "1) 不要过度脑补：没有证据的推断必须写“可能/需要确认”。",
    "2) 输出必须可执行：不要泛泛总结，要给出能立刻行动的方案。",
    "3) 若信息不足：必须在 solutionOutline 里写“需要确认：...”并提出具体问题。",
    "4) 任何情况下都不要输出 Markdown、不要输出代码块、不要输出额外解释文字，只能输出 JSON。",
    "",
    `【对方身份】${identity}`,
    `【身份指导】\n${identityGuides[identity]}`,
    "",
    `【回复形式】${outputFormat ?? "不生成"}`,
    replyGuide(outputFormat),
    "",
    `【人物画像】\n${personHint}`,
    "",
    "【输出 JSON 契约（必须严格遵守）】",
    "必须只输出一个 JSON 对象，字段必须且只能为：realIntent, solutionOutline, risks, reply, personProfileUpdate。",
    "",
    "字段规则：",
    "- realIntent：字符串数组，1~3 条。每条<=35字。写“对方真实想要的结果/态度/动作”。若不确定，用“可能/需要确认”。",
    "- solutionOutline：字符串数组，0~6 条。每条以动词开头（确认/整理/输出/同步/推进/提醒/复盘等）。必须包含：你下一步要做什么 + 时间/节奏（如能推断）。缺信息则加：需要确认：问题1；问题2。",
    "- risks：字符串数组，0~5 条。至少覆盖 2 个维度：范围/时间/依赖/关系/承诺/验收口径/信息缺口。没明显风险可返回空数组。",
    "- reply：字符串或 null。只有当用户选择了微信或邮件时才生成；否则必须为 null。",
    "- personProfileUpdate：字符串或 null。只有当指定了 personName 时才生成；否则必须为 null。",
    "",
    "personProfileUpdate 写作要求（只有指定人物时）：",
    "- 用简短 Markdown（允许换行与小标题），输出“长期画像”，包含：语言风格/偏好、压力信号、边界/雷区、建议回复方式。",
    "- 不要记录隐私信息（手机号、地址、账号、金额等），不要记录一次性任务细节（例如某次具体文件名/金额/具体数据）。",
    "- 80~200 字左右，越精炼越好。",
    "",
    `【文字输入】${message?.trim() ? message.trim() : "无"}`,
  ].join("\n");
}
